/**
 * 判断鼠标坐标是否在对象的范围内
 * @param {number} mouseX
 * @param {number} mouseY
 * @param {object} widget
 * @returns boolean
 */
export function judgeMouseOnWidget(mouseX, mouseY, widget) {
  return true;
}
